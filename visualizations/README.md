# Visualization Scripts

This folder contains Jupyter notebooks and scripts for generating project figures.

## Setup
To run these scripts, you need the visualization dependencies installed. From the **root** of this repository, run:

```bash
pip install -r requirements-vis.txt